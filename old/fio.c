#include <stdio.h>
#include <stdlib.h>

int main (void) {
int i;
/*вводим с клавиатуры данные и читаем их в массив*/
char* data;
char* element;
char** list;
	
 printf(" data = ");
 scanf("%as", &data);
 
   for (i=0, i<strlen(data), i++) {
	  if (data[i]= ' ')
		
		  
	     

 
 
 
 

	
	
	
	
	
	
/*	открываем файл со списокм фамилий
FILE *fp;

if((fp = fopen("fio.txt","r")) == NULL) {
	printf("Cannot open file.\n");
	exit(1); //выйти из проги
 }



  if( fclose( fp )) 
      printf("File  close error.\n");
/*
